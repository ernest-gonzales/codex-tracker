[package]
name = "app_api"
version.workspace = true
edition = "2024"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
tracker_app = { path = "../app" }
tracker_core = { path = "../core" }
tracker_db = { path = "../db" }
ingest = { path = "../ingest" }
